# Core CV
ultralytics>=8.3.0        # YOLO-World detector + ByteTrack tracker
clip @ git+https://github.com/ultralytics/CLIP.git  # required by YOLO-World set_classes()
lap>=0.5.12              # required by Ultralytics ByteTrack
opencv-python>=4.10.0
numpy>=1.26.0
scipy>=1.13.0
shapely>=2.0.0

# Backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
sqlalchemy>=2.0.0
python-multipart>=0.0.9   # file upload
pyyaml>=6.0

# Eval
pandas>=2.2.0
scikit-learn>=1.5.0

# Test
pytest>=8.3.0

# Assistant — optional. Pipeline must run without it (template fallback).
anthropic>=0.40.0
